{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Pre-engine: no plugin permissions needed yet.",
    "windows": ["main"],
    "permissions": [
        "core:default"
    ]
}
